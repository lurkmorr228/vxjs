export const enum EventBinding {
  LOCAL = 'event:local',
  REMOTE = 'event:remote',
  SCRIPT = 'event:script',
}

export const enum ExportBinding {
  LOCAL = 'export:local',
  REMOTE = 'export:remote',
}

export const enum CommandBinding {
  CHAT = 'command:chat',
  CONSOLE = 'command:console',
}
